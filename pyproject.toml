[project]
name = "apify-realestate-au-property-api"
version = "0.1.0"
description = "Example client for the Realestate.com.au Property API Apify Actor"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "apify-client>=3,<4",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
